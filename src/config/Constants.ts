/**
 * General & NestJS constants
 */
export enum Constants {
    // Controllers
    TASKS_PREFIX = 'tasks',

    // Database
    SEQUELIZE = 'SEQUELIZE',

    // Repositories
    TASKS_REPOSITORY = 'TASKS_REPOSITORY',
}